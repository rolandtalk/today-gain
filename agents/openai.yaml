interface:
  display_name: "Today Gain"
  short_description: "Track Taiwan stock gains in Sheets or SQLite"
  default_prompt: "Use $today-gain to extract my holdings and update either the gain sheet or SQLite price database."

policy:
  allow_implicit_invocation: true
